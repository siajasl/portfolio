# import * from "../../shared/Scalars.graphql"

"""Information required to rettrieve a customer's identity verification status.

"""
input GetIdentityVerificationStatusInput {
    "Customer's identifier."
    customerID: ID!

    "Identity verifier's transaction reference."
    verificationTxReference: String!
}
