# import * from "../enums/AssetPair.graphql"

"""Information submitted when requesting the daily exchange volume.

"""
input GetVolumeInput {
    "Asset pair being traded, e.g. ETHBTC."
    assetPair: AssetPairEnum!

    "Exchange for which daily exchange volume is being requested."
    exchange: ExchangeSymbol!
}
