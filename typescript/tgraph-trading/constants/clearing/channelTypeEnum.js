/**
 * @fileOverview Set of atomic swap channel types.
 */

// Enumeration of channel types.
export const ChannelTypeEnum = Object.freeze({
    // First channel opened in atomic swap process.
    INITIATE: 'INITIATE',

    // Second channel opened in atomic swap process.
    PARTICIPATE: 'PARTICIPATE',
});
