/**
 * @fileOverview Set of order book sides.
 */

// Enumeration of order book sides.
export const BookSideEnum = Object.freeze({
    // The buy side, i.e. bids.
    BUY: 'BUY',

    // The sell side, i.e. asks.
    SELL: 'SELL',
});
