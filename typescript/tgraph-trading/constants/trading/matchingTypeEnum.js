/**
 * @fileOverview Set of support metching algorithm types.
 */

// Enumeration of matching algortihms.
export const MatchingTypeEnum = Object.freeze({
    // An algorithm targeting standard matching.
    STANDARD: 'STANDARD',

    // An algorithm targeting MERCHANT trading.
    MERCHANT: 'MERCHANT',

    // An algorithm targeting OTC trades.
    OTC: 'OTC',
});
