/**
 * @fileOverview Wallet footer text defaults.
 */

import * as CANVAS from './canvas';

// Default header text.
export const text = 'accessfile.io';

// Default header text alignment.
export const textAlign = CANVAS.textAlign;

// Default header font.
export const font = '24px ' + CANVAS.fontFamily;

// Default header x position.
export const x = CANVAS.width / 2;

// Default header y position.
export const y = CANVAS.height - CANVAS.padding * 4;
