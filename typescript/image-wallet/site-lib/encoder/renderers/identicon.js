/**
 * @fileOverview Identicon renderer.
 */

import { logTODO } from '../../utils/logging';

/**
 * Header identicon.
 * @param {EncodingContextInfo} ctx - Encoding processing context information.
 */
export default async function(ctx) {
    // Work.
    logTODO('render identicon');
}
